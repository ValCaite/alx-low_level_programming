#include "main.h"

/**
 * _abs - Computes the absolute value of a number
 *
 * @n: The number t be computed
 *
 * Return: The absolute value
 */

int _abs(int n)

{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
